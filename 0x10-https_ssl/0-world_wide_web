#!/bin/bash

# Function to display information about a subdomain
display_info() {
    local domain="$1"
    local subdomain="$2"

    # Resolve the subdomain and get the record type and destination
    local result=$(dig +short "${subdomain}.${domain}" | awk -v sub="${subdomain}" '
        BEGIN { found = 0; record_type = ""; destination = "" }
        /^[[:digit:].]+$/ { if (found) destination = $0; }
        /^;; ANSWER SECTION:$/ { found = 1; next; }
        found && /^[[:alnum:]-]+/ { if (found) record_type = $0; found = 0; }
        END { if (record_type != "") print "The subdomain", sub, "is a", record_type, "record and points to", destination; }
    ')

    if [ -n "$result" ]; then
        echo "$result"
    else
        echo "Subdomain not found: $subdomain"
    fi
}

domain="$1"
subdomain="$2"

if [ -z "$domain" ]; then
    echo "Please provide the domain name as the first argument."
    exit 1
fi

if [ -z "$subdomain" ]; then
    # Display information for default subdomains
    display_info "$domain" "www"
    display_info "$domain" "lb-01"
    display_info "$domain" "web-01"
    display_info "$domain" "web-02"
else
    # Display information for the specified subdomain
    display_info "$domain" "$subdomain"
fi
